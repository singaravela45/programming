In Mongoose, you use the model name (User) to access functions
In MongoDB, you use the collection name (users) directly
Mongoose model name maps to MongoDB collection — usually auto-pluralized and lowercased
