wsl 

wsl --list --verbose               see which is running
wsl --shutdown                    stop all os
wsl --terminale ubuntu            stop only ubuntu
wsl --set-default kali-Linux       set kali as default
wsl --status                       
wsl --unregister Debian          removes Debian
wsl -d kali-linux

to run kali environment :
first sudo apt install -win-kex -y  
when ever need: kex
to stop:kex--win--stop